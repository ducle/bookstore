%h1
  List Books
  %a.btn.btn-primary.btn-lg.float-right{href: new_book_path}
    %i.fa.fa-plus
    Add new

%table.table.table-striped.table-bordered
  %thead
    %tr
      %th
        Category
      %th
        Title
      %th
        Price
      %th
  %tbody
    - @books.each do |book|
      %tr
        %td
          = book.category_name
        %td
          = book.title
        %td
          = number_to_currency book.price
        %td
          = link_to book_path(book), class: 'btn btn-success' do
            %i.fa.fa-eye
          &nbsp;
          = link_to edit_book_path(book), class: 'btn btn-warning' do
            %i.fa.fa-pencil
          &nbsp;
          = link_to book_path(book), method: :delete, data: {confirm: "Are you sure you want to delete this book"}, class: 'btn btn-danger' do
            %i.fa.fa-times
